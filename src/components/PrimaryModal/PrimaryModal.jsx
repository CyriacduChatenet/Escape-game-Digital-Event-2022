import React from 'react';

const PrimaryModal = () => {
    const [showModal, setShowModal] = useState(false);

    return ( 
        <div className='modal'>

        </div>
     );
}
 
export default PrimaryModal;